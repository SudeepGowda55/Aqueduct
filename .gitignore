# Compiler files
cache/
out/

# Ignores development broadcast logs
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# Node
node_modules/
pnpm-lock.yaml

# Subgraph / keeper build output
subgraph/generated/
subgraph/build/
keeper/dist/

# Frontend build output.
frontend/.next/
# NOTE: frontend/public/deployment.json is deliberately NOT ignored -- it now records the real,
# permanent Base Sepolia deployment (contracts don't move), not ephemeral local anvil state.

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.vscode/
.idea/
